import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  appBarSearch: {
    marginTop: "30px",
    display: "flex",
    alignItems: "center",
    flexDirection: "column",
  },
  input: {
    width: "550px",
  },
}));
